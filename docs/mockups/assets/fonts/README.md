# Mockup Fonts

This folder contains candidate display fonts for MusterDeck mockups.

Keep each font's license file with the font file. Do not move these into production until the visual brand guide approves them and the production font folder is created under `src/assets/fonts/`.

Current candidates:

- `capture-it/`
- `take-cover/`

